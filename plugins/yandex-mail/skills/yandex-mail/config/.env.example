# yandex-mail — дополнительные настройки (если нужны)
# Общий Yandex-токен берётся из ~/.claude/secrets/yandex-app.json — здесь его НЕ дублируем.
# Сюда кладём только параметры специфичные для этого скилла.
